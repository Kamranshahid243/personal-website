import type { Faq } from "@/types/content";

/**
 * Objection handling for the contact and services pages.
 *
 * Answering "what does it cost", "how fast can you start" and "do you work
 * with agencies" in public removes the most common reasons a lead never sends
 * the first message. Also eligible for an FAQPage rich result.
 */
export const faqs: Faq[] = [];
