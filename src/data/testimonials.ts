import type { Testimonial } from "@/types/content";

/**
 * Social proof.
 *
 * Attributed quotes only — name, role and company. An anonymous testimonial is
 * worse than none, because it signals the rest may be invented too.
 */
export const testimonials: Testimonial[] = [];
